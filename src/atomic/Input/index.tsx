import React from 'react'
import styles from '@styles/atomic/Input.module.scss'

type Props = {}

const Input = (props: Props) => {
  return (
    <div className={styles.text} >Input</div>
  )
}

export default Input